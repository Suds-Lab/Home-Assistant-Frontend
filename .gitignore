# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets & local config - never publish
.env

# Dev-only scratch helpers (kept locally, e.g. the mock Home Assistant)
_*.py
_*.json

# Runtime data (lives in /data on the add-on; dev writes it into my_home/)
my_home/settings.json
my_home/activity.json
my_home/app-icon.*

# Misc
*.log
.DS_Store
